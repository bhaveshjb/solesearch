<html>
  <body>
    <div>
    <% if(user && user.is_authenticated){ %>
       Hello  <%= user.login %>!
      <a href="{{ url_for('logout_view') }}">Logout</a>
      <% }else{%>
        Welcome anonymous user!
      <a href="/admin-panel/login-page">Login</a>
    <% }%>
    </div>
    <div>
      <a href="{{ url_for('admin.index') }}">Go to admin!</a>
    </div>
  </body>
</html>
